OFCA — Optical Flow Cluster Analyzer
===================================

Quick start
-----------
1) Create venv & install deps:
   python -m venv .venv && . .venv/bin/activate  # on Windows: .venv\Scripts\activate
   pip install -r requirements.txt

2) Run:
   python main.py

Project layout
--------------
See the /ofca package for modules.
